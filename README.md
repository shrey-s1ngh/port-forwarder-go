### TCP port forwarder written in GO
- This mini project uses net package of go
- It just binds the request your own localhost (with configured port) and forwards the request to any third resource (any unreachable website...).
